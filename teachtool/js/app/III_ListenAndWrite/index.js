var Section_ListenAndWrite = {
    Render: function (sectionName) {
        //获取section数据
        var sectionData = Model.GetData(sectionName);

        $("#title").html("Section_ListenAndWrite_Render");
    }
};